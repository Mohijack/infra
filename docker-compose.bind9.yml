services:
  bind9:
    image: ubuntu/bind9:9.18-22.04_beta
    container_name: bind9
    ports:
      - "5353:53/udp"
      - "5353:53/tcp"
    volumes:
      - ./bind9:/etc/bind
    networks:
      - traefik

networks:
  traefik:
    external: true